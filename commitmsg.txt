fix(phone-index): evitar UIDs duplicados + reset agressivo (#resetar)

UID resolver agora varre phone_index, UID existente e perfis (child/guardians) antes de criar novo.
#resetar remove duplicados relacionados, recria conta zerada e remapeia números → UID canônico.
