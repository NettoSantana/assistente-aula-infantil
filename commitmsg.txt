fix(phone-index): unificar UIDs duplicados automaticamente no webhook

- Resolve UID canônico pelo número recebido
- Merge de contas-espelho (phone_index + perfis) → UID estável
- Remapeia todos os números para o UID canônico
- #resetar agora usa a mesma estratégia de unificação antes de zerar
